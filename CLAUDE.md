# 다산동 반려동물 산책 SNS — 공통 규칙

프로젝트 배경·기능 원안·결정/미정 사항은 [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md) 참고.
이 파일은 "어떻게 코드를 작성할지"에 대한 규칙만 다룬다.

## 현재 단계

프로토타입 단계. 지도가 뜨고 GPS 경로를 그려보는 정도의 검증이 목표이며, 아직 실제 사용자가
붙는 서비스가 아니다. 회원/백엔드가 필요한 기능(로그인, 다른 사용자 경로 조회, 미션/리워드 저장)은
[PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)의 "미정 사항"이 정리되기 전까지 실제로 연결하지 않는다 —
UI만 먼저 만들고 데이터는 로컬(localStorage)에 임시로 둔다.

## 기술 스택 원칙

- 순수 HTML/CSS/JS, 빌드 도구 없음. 화면은 단일 `.html` 파일로 완결 (필요해지면 분리 논의).
- 지도: Leaflet + OpenStreetMap 타일 (CDN `<script>` 태그, 가입/API 키 불필요). 원래 Mapbox GL JS를
  검토했으나 무료 티어도 카드 등록이 필요해 배제함(2026-09-02 결정, [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)
  참고) — 실제 서비스 단계에서 상업적 트래픽 정책이나 세밀한 커스텀 스타일이 필요해지면 재검토.
- PWA: `manifest.json` + 서비스워커로 "홈 화면에 추가" 정도까지만 우선 지원. 오프라인 캐싱 등은
  필요해지면 그때 추가.
- GPS 추적은 `navigator.geolocation.watchPosition` 기반. 브라우저 권한 요청 UX(왜 위치가
  필요한지 설명)를 반드시 포함한다.

## 텍스트 규칙

- UI 문구, `prompt()`/`alert()`/`confirm()` 메시지, 주석은 한국어.
- 변수/함수명은 영어.

## 개인정보 취급

이 서비스는 사용자의 실시간 위치(산책 경로)를 다룬다 — `projectA`의 고객 데이터보다도 훨씬
민감할 수 있다 (누군가의 집 근처 이동 패턴이 노출될 수 있음). 위치 데이터를 외부로 전송하는
코드(백엔드 API 호출 등)를 추가하기 전에는 반드시 무엇을, 어디로, 누구에게 공개되는 형태로
보내는지 먼저 설명하고 확인받는다. 테스트 시 실제 자기 집 주소 근처에서 실제 경로로 캡처/커밋하지
않는다 — 스크린샷이 필요하면 가짜 경로(예: 임의 좌표)로 만든다.

## 새 기능을 추가할 때

새로운 화면/기능이 생기면 필요 시 하위 폴더에 별도 `CLAUDE.md`를 만들어 세부 규칙을 적는다.
공통 규칙은 이 파일에만 두고 중복 작성하지 않는다.
