# 다산동 반려동물 산책 SNS — 프로젝트 컨텍스트

## 배경

다산동 아웃렛 주변에 반려동물을 데리고 오는 사람이 많다는 관찰에서 출발한 신규 서비스 아이디어.
반려동물 산책을 기록하고 공유하는 "산책 다이어리 + SNS" 컨셉으로 가볍게 시작한다.

이 문서는 앞으로 이 프로젝트에서 내려진 결정과 아직 미정인 사항을 계속 갱신해가며 관리한다
(형식은 `projectA`의 `PROJECT_CONTEXT.md` 운영 방식을 참고).

## 핵심 기능 (사용자가 제시한 원안)

- **산책 경로 기록**: 산책 시작 시 앱을 켜두면 GPS로 이동 경로를 지도 위에 실시간 기록
- **핀 꽂기**: 산책 중 특정 지점에 핀을 남겨 흔적을 표시 (배변 봉투함, 쉼터, 특이사항 등 자유 기록으로 추정 — 정확한 핀 종류/용도는 미정)
- **흔적 공유/열람**: 내가 남긴 산책 흔적을 다른 반려동물 보호자들도 주변에서 확인 가능 — 반려동물 버전 SNS
- **경로 공유 기능**: 산책 경로를 다른 사용자와 공유
- **리텐션 장치**: 자주 접속/기록하면 리워드, 숨겨진 미션(히든 미션)을 해결하면 리워드 지급

> 위 기능 목록은 사용자가 대화에서 제시한 원안 그대로이며, 세부 스펙(핀 종류, 공유 범위,
> 미션 설계, 리워드 종류/지급 방식)은 아직 확정되지 않았다. 아래 "미정 사항"에서 계속 추적한다.

## 지도 위 핀 표시 방식 (2026-08-26 결정)

지도에 핀이 여러 개 붙었을 때 "이게 누구 핀인지" 한눈에 구분되고, 산책 카드에서 만든 칭호가
지도 위에서도 성취감으로 드러나도록 차별화하자는 취지로 논의 → 채택:

- **기본**: 이름 + 반려동물 사진을 핀 위에 표시.
- **칭호가 있으면**: 칭호도 같이 붙여서 보여준다 (없으면 이름+사진만).

**성능 고려사항 (개발자가 짚고 사용자가 동의)**: 핀마다 사진을 항상 로드/렌더링하면 핀 개수가
많아지거나 지도를 줌아웃했을 때 무거워질 수 있다 — 클러스터링하거나 줌 레벨에 따라 사진
표시 여부를 조절하는 걸 함께 고려해야 한다. 구체적으로 어느 줌 레벨부터 사진을 숨기고
단순 점(dot)으로 바꿀지, 클러스터링 라이브러리를 쓸지(Mapbox GL의 `cluster` 옵션 등)는
아직 미정 — 실제 Mapbox 지도가 붙고 핀이 여러 개 생기기 시작하면 구체화할 것.

핀에 표시할 "칭호"가 산책 카드의 획득 칭호(`BADGES` 카탈로그, 아래 "획득 칭호 카탈로그 추가"
참고)와 같은 걸 가리키는지, 아니면 지도 전용 칭호 개념이 따로 있는지는 아직 명시적으로
안 정해서 — 실제 핀 UI를 구현할 때 다시 확인이 필요하다.

## 지도 연출 기획: "동화책 속 산책 지도" 3가지 장치 (2026-08-27 제안)

색감 외에 지도 자체를 "산책하고 싶은 공간"으로 보여주기 위한 연출 3가지를 사용자가 제안함
(원문 그대로 보존 — 실제 구현 시 이 톤/디테일을 기준으로 삼는다):

1. **산책 코스/공원 영역 강조**: 다산 중앙공원이나 수변공원 같은 녹지 영역 위로 반짝이는
   애니메이션 이펙트나 "🌿 추천 산책로" 깃발 핀을 세워둔다.
2. **발자국 잔상 레이어 (Footprint Trail)**: 유저가 실제 걸어간 길 뒤로 귀여운 강아지 발자국
   🐾가 점선으로 콕콕 찍히는 연출을 넣으면 걸으면서 지도를 보는 재미가 생긴다.
3. **"지금 산책 중인 동네 친구" 실시간 카운트 뱃지**: 지도 상단에
   `[🐾 지금 다산동에 14마리 댕댕이가 산책 중!]` 팝업을 띄워서 "우리 아이도 지금 나가서 친구들
   만나게 해줘야지"라는 동기부여를 준다.

아직 구현 전이고, 개발자 검토 결과 세 가지의 구현 난이도/전제조건이 서로 다르다:

- **1번(추천 산책로 강조)·2번(발자국 잔상)**: 순수 시각 연출이라 클라이언트에서 바로 프로토타입
  가능 — 배경/백엔드 없이도 만들어볼 수 있음. 다만 "녹지 영역"을 어떻게 정의할지(Mapbox 스타일의
  랜드유즈 레이어를 활용할지, 다산 중앙공원/수변공원 좌표를 수동으로 폴리곤으로 찍어둘지)는
  아직 미정. 발자국 잔상은 실시간 GPS 경로 기록(`index.html`의 `watchPosition` 기반 경로 그리기)
  위에 얹는 오버레이라 기존 경로 그리기 기능과 자연스럽게 연결됨.
- **3번(실시간 동네 친구 카운트)**: 이름 그대로 "지금 이 순간 다른 사용자가 몇 명 산책 중인지"
  실제 데이터가 필요한데, 이 프로젝트는 아직 회원/백엔드가 없는 프로토타입 단계라(위 "미정 사항"
  참고) 실제 값을 낼 방법이 없다. `CLAUDE.md`의 "회원/백엔드가 필요한 기능은 실제로 연결하지
  않는다" 원칙에 따라, UI만 목업(가짜 숫자)으로 먼저 만들어볼 수는 있지만 실제 카운트 연동은
  백엔드/실시간 동기화 방식이 정해진 뒤에나 가능하다.
- Mapbox Access Token이 아직 없어서(위 "다음에 필요한 것" 참고) `index.html`의 지도 자체가
  실제로 떠 있는 상태는 아직 아님 — 이 세 장치를 실제로 눈으로 확인하려면 토큰 발급이 먼저
  필요하다.

## 톤앤매너: B급 감성 (2026-08-26 결정)

프로젝트 전체를 관통하는 톤은 "B급 감성"이며, 이를 주로 드러내는 곳은 **카드에 들어가는 텍스트**다
(UI 문구 전반으로 확대할지는 아직 미정 — 우선은 산책 카드 텍스트에 집중).

구체적으로 스킬 칸은 "제목 + 부연 설명"의 2단 구조로 쓴다. 제목에는 드립스러운 이름과 함께
게임 스탯처럼 보이는 괄호 표기(쿨타임/소모 HP/지속 시간 등, 전부 드립용 가짜 수치)를 붙이고,
설명에는 실제 반려동물 행동을 관찰한 듯한 서술 + 확률/수치 드립을 곁들인다.

사용자가 준 원본 예시 (그대로 보존 — 카드 텍스트를 쓸 때 이 톤을 기준으로 삼는다):

> **식탁 밑 아련한 눈빛 빔** (소모 HP: 0)
> 사람이 밥 먹을 때 턱을 무릎에 얹고 불쌍함 스탯을 최대로 끌어올린다. (고기 한 점 획득 확률 88%)
>
> **귀신같은 비닐 소리 감지** (쿨타임: 0초)
> 아무리 깊은 잠에 빠져 있어도 간식 비닐 소리가 나는 순간 0.1초 만에 눈을 번쩍 뜬다.
>
> **수제간식점 앞 얼음 놀이** (지속 시간: 보호자 설득 전까지)
> 단골 간식집 문 앞을 지나갈 때 발바닥에 접착제를 바른 듯 문 앞에 멈춰 선다.

`card.html`의 스킬 1·2 입력 필드를 제목/설명 두 칸으로 분리해 이 형식을 반영함 (아래 "결정된 사항" 참고).

### 카드 구성: 액티브 스킬 2개 + 패시브 1개 (2026-08-26 결정)

한 번 스킬 3개로 만들어봤다가, 액티브 스킬 2개 + 패시브 1개 구조로 정리했다. 패시브는 액티브
스킬과 달리 괄호 안 가짜 스탯 표기가 없고 `[제목]: 설명` 형태이며, 카드에서도 점선 테두리 +
다른 색으로 액티브 스킬과 구분해서 그린다.

사용자가 준 패시브 원본 목록 (`card.html`의 드롭다운에 그대로 반영, 그대로 보존):

> [선택적 귓등 듣기]: "간식", "산책" 소리엔 0.1초 만에 반응하지만, "안돼", "기다려"는 주파수가
> 안 맞는다며 무시함.
>
> [목욕 후 미친개 모드]: 목욕을 마치고 털을 말리는 순간 억울함이 폭발하여 온 거실 이불을 핥으며
> 미친 듯이 슬라이딩함.
>
> [남의 떡이 더 커 보임]: 똑같은 간식을 줘도 꼭 다른 친구 강아지 입에 물린 간식을 아련하게 바라봄.
>
> [새 옷 착용 시 1초 멘붕]: 예쁜 파스텔 옷을 입혀놓으면 고장 난 로봇처럼 제자리에 굳어서 움직이지
> 않음.
>
> [침대 중앙 점령권]: 같이 잘 때 정중앙에 'T'자로 누워 보호자를 침대 구석으로 밀어냄.
>
> [비 오면 발에 물 묻히기 싫음]: 산책 나가자고 떼써서 나갔더니 현관문 앞 빗물 보고 0.5초 만에
> 안아달라고 칭얼거림.
>
> [발바닥 고소함 MAX]: 꼬질꼬질한 꼬꼬마 발바닥에서 구수한 옥수수 칩 냄새가 진동함.
>
> [터그놀이 집착증]: 실타래 토이를 물려주면 목을 꺾어가며 으르렁대지만 기분은 최고조임.
>
> [배변패드 모서리 슛]: 정중앙을 두고 꼭 모서리에 싸서 보호자의 깊은 한숨을 유발함.
>
> [산책 5분 후 안아라 신호]: 신나게 뛰다가 갑자기 멈춰 서서 두 앞발로 다리를 긁으며 안아달라 함.
>
> [외부인 한정 순둥이]: 집에서는 왕처럼 굴다가 병원이나 훈련사 앞에서는 세상 착한 순한 양이 됨.

프로토타입 단계에서는 이 11개를 드롭다운으로 골라 채우는 방식(직접 수정도 가능)이고, 실제
서비스에서 반려동물마다 패시브를 어떻게 배정/획득하게 할지(랜덤 뽑기인지, 행동 기록 기반인지)는
아직 미정 — "미정 사항"의 미션/리워드 설계 항목과 연결된다.

### 스킬 카탈로그 (2026-08-26 추가)

패시브와 마찬가지로 스킬 1·2도 드롭다운으로 고르거나 직접 입력할 수 있게 했다. 사용자가 3개
카테고리로 나눠 준 스킬 목록 (`card.html` 드롭다운에 카테고리별 그룹으로 그대로 반영, 원문 보존):

**[생존 & 간식 계열]**
> 식탁 밑 아련한 눈빛 빔 (소모 HP: 0) — 사람이 밥 먹을 때 턱을 무릎에 얹고 불쌍함 스탯을 최대로
> 끌어올린다. (고기 한 점 획득 확률 88%)
>
> 귀신같은 비닐 소리 감지 (쿨타임: 0초) — 아무리 깊은 잠에 빠져 있어도 간식 비닐 소리가 나는
> 순간 0.1초 만에 눈을 번쩍 뜬다.
>
> 수제간식점 앞 얼음 놀이 (지속 시간: 보호자 설득 전까지) — 단골 간식집 문 앞을 지나갈 때
> 발바닥에 접착제를 바른 듯 문 앞에 멈춰 선다.

**[산책 & 행동 계열]**
> 천사표 얼굴로 배변 포즈 (보호자 민망함 +50) — 산책 중 세상에서 가장 무해하고 아련한 눈빛으로
> 보호자를 응시하며 응가 포즈를 잡는다.
>
> 3초 멈칫 냄새 탐정 (속도: -100%) — 산책 중 맘에 드는 풀숲을 발견하면 보호자를 3분간 자리에
> 얼음으로 만든다.
>
> 배달 오토바이 레이더 (방어력: +30) — 라이더 삼촌 오토바이 소리가 단지 입구에 들어서는 순간
> 컹컹 경보를 울린다.

**[일상 & 앙탈 계열]**
> 빗질 거부 온몸 비틀기 (회피율: +99%) — 슬리커 빗만 들면 뼈가 없는 오징어로 변신하여 손가락
> 사이로 탈출한다.
>
> 발톱 깎기 소파 밑 침투 (은신 기술) — 바리깡 소리가 들리는 순간 빛의 속도로 소파 밑 가장
> 어두운 구석으로 침투한다.

카테고리(생존/산책/일상)가 실제로 카드 등급이나 속성 태그와 연결되어야 하는지는 아직 미정 —
지금은 그냥 드롭다운 그룹 라벨로만 쓰인다.

## 카드 시안을 여러 파일로 관리하기로 함 (2026-08-26 결정)

사용자가 "카드 시안이 앞으로 다양하게 생길 것 같다"며 시안별로 파일을 늘려 비교해보자고 제안 →
채택. 화면 하나 = 단일 `.html` 파일 원칙([루트 CLAUDE.md](CLAUDE.md))은 유지하되, 여러 시안이
스킬/패시브 문구 카탈로그나 텍스트 줄바꿈 같은 로직을 그대로 복사하면 하나 고칠 때 다른 파일에서
놓치는 사고가 나기 쉬워서, 공통 로직만 `card-shared.js`로 분리했다 (빌드 도구 없이
`<script src="card-shared.js">` 태그로만 불러옴 — 전역 `window.CardShared` 객체).

- `card-shared.js`로 뺀 것: SKILLS/PASSIVES 문구 카탈로그, 텍스트 줄바꿈(wrapText 등), 스킬/패시브
  칩 그리기(drawTraitChip), 드롭다운 채우기(buildCatalogSelect), 사진 드래그 처리, PNG 저장.
- 각 시안 파일에 그대로 남겨둔 것: 실제 레이아웃(캔버스에 뭘 어디에 그릴지), 카드 프레임 디자인,
  폼 필드 구성 — 시안마다 다른 게 당연한 부분이라 공유하지 않음.
- 새 시안을 추가할 때 이 구조를 그대로 따르면 된다: `<script src="card-shared.js">`를 먼저 불러오고
  `CardShared.*`를 사용, 시안 고유의 레이아웃/디자인만 해당 파일에 작성.
- 시안 파일끼리는 각 파일 상단에 "시안 A / 시안 B / ..." 식으로 서로 링크하는 네비게이션을 넣어
  비교하기 쉽게 해뒀다 (`card.html`, `card-keyring.html`, `card-story.html`, `card-stamp.html`).
  새 시안을 추가하면 기존 파일들의 네비게이션에도 링크를 추가해야 한다 (자동 아님, 수동 동기화).

**`card-shared.js` 캐시 주의사항 (2026-08-26 발견)**: 브라우저가 `card-shared.js`를 꽤 공격적으로
캐싱해서, 파일을 수정해도 이미 열어둔 페이지가 예전 버전을 계속 쓰는 경우가 있었다 (테스트 중
발견 — 새로고침/새 탭을 열어도 재현됨). `<script src="card-shared.js?v=2">`처럼 쿼리스트링으로
버전을 박아뒀으니, `card-shared.js`를 고칠 때는 이 버전 번호를 올리고 그걸 참조하는 모든 카드
파일에서 같이 올려야 한다 (현재 4개 파일 전부: `card.html`, `card-keyring.html`, `card-story.html`,
`card-stamp.html`).

## 시안 B: 키링형 (실물 출력용) — `card-keyring.html` (2026-08-26)

사용자가 카드를 실제로 인쇄해서 키링/아크릴 굿즈로 만들 계획이라, 작은 실물 크기에서도 읽히도록
퀄리티를 보완한 시안. 사용자가 제시한 방향 그대로 반영:

- **사진 대폭 확대**: 원형 사진이 카드의 절반 이상을 차지. 사진이 카드 위쪽 골드 프레임을 살짝
  넘어가는 "팝업" 효과를 넣음.
  - ⚠️ **제약**: 진짜로 귀/털이 배경 없이 튀어나오는 효과는 반려동물 사진에서 배경을 제거한
    실루엣(투명 배경 PNG)이 있어야 한다. 브라우저에서 임의의 사진을 자동으로 배경 제거하는 건
    품질이 들쭉날쭉해서 실물 굿즈용으로 쓰기 어렵다고 판단해, 대신 사진을 원형으로 잘라 프레임
    위로 겹쳐 걸치는 "윈도우 컷" 방식으로 시뮬레이션함 (개발자 판단 — 사용자 확인 필요). 진짜
    실루엣 컷아웃이 필요해지면 사용자가 미리 배경을 제거한 투명 PNG를 업로드하는 방식을 열어둘 수
    있음 (아직 미구현).
- **텍스트 영역 축소**: 스킬 2개 + 패시브 1개(4개 텍스트 박스) → 주력 스킬 1개 + B급 특성 1개,
  총 2개로 줄임. 산책거리/HP는 처음엔 상단에 작은 알약형 배지 두 개(❤ HP, 🚶 거리)로 압축했었으나,
  나중에 "데이터가 산만하다"는 피드백으로 아예 뺌 (아래 "정보 축소 + 스킬 텍스트 중심으로 재정리" 참고).
- **두꺼운 테두리감**: 골드 그라데이션 이중 테두리 + 네 귀퉁이에 발바닥 아이콘 코너 메달을 넣어
  "수집 카드" 느낌을 강조.
- **버그로 배웠던 것**: 팝업 영역(사진이 프레임 위로 튀어나온 부분)에 그림자가 없어서 "그냥 빈
  여백처럼 보인다"는 사용자 피드백을 받음 — 확인해보니 배경색 자체는 정상(페이지 배경과 동일한
  크림색)이었지만, "떠 있다"는 시각적 단서(그림자)가 없어서 비어 보였던 것. `drawPhoto()`에서
  사진을 그리기 전에 같은 원 모양으로 옅은 그림자(`ctx.shadowBlur`/`shadowOffsetY`)를 먼저 깔아서
  해결함 (2026-08-26). 새로 "떠 있는" 느낌의 요소를 추가할 땐 이 패턴(그림자 먼저 → 실제 내용)을
  참고할 것.

**정보 축소 + 스킬 텍스트 중심으로 재정리 (같은 날, 사용자 피드백)**: "데이터가 너무 많아 산만하다"는
지적으로 HP 바/산책거리 배지/별 등급(rarity)을 전부 뺐다 (폼 필드도 같이 제거 — `card.html`에는
그대로 남아있음, 시안별로 필요한 데이터가 다르다는 전제). 지금 이 시안은 이름·레벨·속성 한 줄 +
스킬 1개 + 패시브 1개 + 칭호만 남아서, 스킬 텍스트가 카드의 실질적인 주인공이 됨. 이에 맞춰
`CardShared.drawTraitChip()`에 두 가지 옵션을 추가함(시안 A는 그대로 기본값 사용, 이 옵션들 안 씀):
- `titleAlign: 'left'` — 제목을 가운데 대신 왼쪽 정렬 (이 시안만 사용, A는 계속 가운데 정렬)
- `descFont` — 설명 글꼴을 시안별로 오버라이드. A는 살구색 배경 가독성 때문에 굵게(600) 키운 게
  맞았지만, 이 시안은 "진하면서 길면 오히려 눈이 아프다"는 반대 피드백을 받아서 굵기를 보통으로
  낮추고 크기(18px)만 키움. **새 시안에서 설명 텍스트 스타일이 A/B와 다르게 필요하면 이 패턴대로
  `descFont`를 넘길 것 — 공용 함수 안의 기본값을 직접 바꾸면 다른 시안이 다 같이 바뀌어버림.**
- 아이콘(⚡/🌙)이 글자랑 위아래로 안 맞아 보인다는 피드백도 같이 고침 — `drawTraitChip` 안에서
  고정 오프셋 대신 그 줄의 정중앙(`textBaseline: 'middle'`)에 아이콘/글자를 같이 맞추도록 바꿔서,
  두 시안 다 자동으로 개선됨.
- (같은 날, 바로 이어서) 제목도 "두껍게 하지 말고 크기만 키우자"는 피드백을 받음 — Black Han
  Sans는 굵기가 하나뿐(항상 두꺼움)이라 이 시안은 제목 폰트 자체를 일반 굵기(맑은 고딕 23px)로
  바꿔서 대응. 그래서 `titleFont`/`titleLH` 옵션도 추가함(A는 여전히 Black Han Sans 19px 기본값).
  **결과적으로 이 시안(B)은 제목·설명 둘 다 A와 다른 폰트/굵기를 쓴다** — 제목 `titleFont`,
  설명 `descFont`를 늘 같이 넘겨야 스타일이 일관되게 나옴 (하나만 넘기면 다른 하나는 A 기본값으로
  섞여서 어색해질 수 있음). 획득 칭호(`🏅 ...`) 줄도 같은 이유로 Black Han Sans → 맑은 고딕
  보통 굵기로 맞춤 — 이 시안은 제목/설명/칭호 전부 Black Han Sans를 안 쓴다.

**배경/테두리 고급화 (같은 날)**: "배경이 밋밋하다"는 지적으로 두 가지 추가함 (개발자가 두
방향을 제안하고 사용자가 승인) —
- 카드 몸통 안에 발자국(🐾) 워터마크를 아주 옅게(투명도 0.07) 흩뿌림. 벽돌쌓기 식으로 줄마다
  반 칸씩 어긋나게, 각도도 위치에 따라 조금씩 다르게 줘서 기계적인 격자 느낌이 안 나게 함
  (`drawBackgroundPattern()`, 카드 몸통 둥근 사각형에 클립해서 밖으로 안 삐져나가게 함).
- 골드 테두리 그라데이션을 3스탑(밝음-어두움-밝음)에서 5스탑(어두운 골드 → 밝은 골드 → 거의
  흰색 하이라이트 → 밝은 골드 → 어두운 골드)으로 바꿔서, 대각선으로 빛이 반사되는 듯한 금박
  느낌을 냄. 이 시안 전용(`card-keyring.html` 안에서만 바뀜, 공용 함수 변경 아님).

## 시안 B-1: 라운드 사각형 (원형 → 가로로 길고 세로 짧은 사각형) — `card-keyring-b1.html` (2026-08-27)

사용자가 원형 vs 가로 타원형 프레임을 비교해보고 싶다고 해서(개발자가 별도 목업 페이지로
시각 비교를 제공) 둘 다 채택하지 않았고, 처음엔 "사진을 카드 배경 전체에 채우기"로 잘못
이해해서 그렇게 구현했다가 — 사용자가 "b-1 다시 설명할게"로 정정: **원형을 없애고, 가로로
길고 세로는 짧은, 모서리만 살짝 둥근 사각형으로 바꾸는 것**이 원래 의도였음. 나머지는 전부
시안 B 그대로(사진이 프레임 위로 튀어나오는 "윈도우 컷" 팝업 방식, 이름/스킬/특성/칭호는
사진 아래 그대로) — 바뀌는 건 사진 영역의 **모양**뿐. 시안 B에서 파생된 새 시안이라 B-1로
명명하고 별도 파일로 분리(기존 "카드 시안은 시안마다 파일 분리" 원칙 유지).

- `card-keyring.html`의 원형 관련 상수/드로잉만 사각형으로 교체: `PHOTO_D`(지름) →
  `PHOTO_W`/`PHOTO_H`(가로 580 · 세로 300 → 같은 날 "세로 영역 2배로" 요청으로 600, 이제 거의
  정사각형에 가까움), 모서리 반경 `PHOTO_R=20`("살짝"만
  둥글게). 그림자(`ctx.arc` → `roundRect`), 클립, 이중 테두리(원형 이중 링 → 이중 라운드 사각형
  스트로크)도 전부 사각형 버전으로 바꿨고, 팝업 위치(`PHOTO_TOP=20`, `FRAME_TOP=90`)는 시안 B와
  동일하게 유지해서 "프레임 위로 살짝 튀어나오는" 효과가 그대로 남는다.
  드래그 hitTest도 원형 거리 판정에서 사각형 범위 판정으로 바꿈. `CS.computePhotoFit()`은
  원래 사각형 rect 기준으로 동작하는 범용 함수라 모양이 바뀌어도 그대로 재사용 가능했음.
- 첫 시도(풀포토 배경)에서 추가했던 `card-shared.js`의 `drawTraitChip()` `bgOpacity` 옵션은
  이 방향(사진이 배경 전체가 아님)엔 필요 없어져서 되돌림 — 사용하지 않는 옵션을 남겨두지
  않기 위해 원래 하드코딩값(0.6)으로 복원. `card-shared.js`를 두 번 고쳤으므로(추가 후 되돌림)
  캐시 버전을 v=6 → v=7 → v=8로 5개 파일 전부 같이 올림.
- 스킬/특성 제목 폰트·색·크기 콤보박스, 획득 칭호 카탈로그 드롭다운 등 B의 다른 기능은 그대로
  재사용(같은 폼 구조·같은 JS 로직).
- 검증: 동심원/강아지 얼굴 테스트 이미지로 사진이 정확히 사각형 범위(580x300, 좌표
  50,20~630,320)에서만 보이고 그 아래 카드 배경 영역에서는 안 보이는지 픽셀 스캔으로 확인,
  휠 줌 동작 확인, PNG 저장 확인, 기존 시안 A/B가 `bgOpacity` 원복 후에도 캔버스 높이가 그대로인지
  재확인함. 실제 렌더링 결과는 스크린샷 대신 캔버스를 PNG로 직접 추출해 사용자에게 파일로
  전달해 확인시킴(이번 세션 스크린샷 도구가 특히 더 불안정했음).
- 5개 카드 파일(A/B/B-1/C/D) 전부의 상단 네비게이션 라벨을 "시안 B-1 · 라운드 사각형"으로 맞춤.

**세로 2배 + 팝업 제거(같은 날, 이어진 요청)**: 먼저 "사진 세로 영역을 지금의 2배로"
요청으로 `PHOTO_H`를 300 → 600으로 늘림(가로 580과 거의 정사각형). 바로 이어서 "사진 영역이
테두리 안으로 들어가게 하고, 윗여백이 좌우 여백에 어울리게 높이를 조절해달라"는 요청 →
시안 B에서 그대로 가져왔던 "프레임 위로 튀어나오는 팝업" 방식 자체를 없앰:
- `FRAME_TOP`을 90(팝업을 위해 남겨뒀던 위쪽 여백) → 14(좌우 여백과 동일)로 바꿔서, 프레임
  자체가 캔버스 위쪽에 바로 붙게 함(이제 위/좌/우 프레임 바깥 여백이 전부 14로 대칭).
- 사진과 프레임 사이 여백을 `PHOTO_PAD=40` 하나로 통일해서 위/왼쪽/오른쪽에 똑같이 적용하고,
  `PHOTO_W`/`PHOTO_H`를 이 여백을 뺀 나머지 값(572)에서 **정사각형**으로 도출함 — "높이를
  여백에 맞게 조절"이라는 요청을 여백 3면을 균등하게 만들고 그 결과로 나온 정사각형으로 해석.
  `PHOTO_TOP = FRAME_TOP + PHOTO_PAD`로 계산해서 사진이 더 이상 프레임 밖으로 튀어나오지 않고
  완전히 안쪽에 들어감.
- 그림자·이중 테두리(흰 링+골드 링) 효과는 "떠 있는 느낌"을 위한 것이었지만, 안쪽으로 들어간
  뒤에도 "사진 인서트" 느낌을 살리는 데 나쁘지 않아 보여서 일단 그대로 뒀음 — 사용자가 안
  어울린다고 하면 다음에 뺄 수 있음.
- 검증: 사진 시작 전(위/좌/우 40px 여백 구간)엔 프레임 배경색이, 그 안쪽엔 사진이 나오는지
  픽셀 스캔으로 3면 다 확인(대칭 검증). PNG 저장 확인. 렌더링 결과는 이번에도 캔버스를 PNG로
  추출해 실제 파일로 사용자에게 전달함.

## 시안 C: 스토리 공유형 — `card-story.html` (2026-08-26)

"소장용 카드"인 A/B와 달리, 인스타그램/카톡 **스토리에 그대로 올리는 용도**로 만든 시안 — 개발자가
제안하고 사용자가 채택. 캔버스가 9:16(720x1080 아님, 720x1280) 고정 비율이고, 다른 시안과 달리
내용 길이에 맞춰 캔버스 높이를 늘리지 않는다(스토리 규격을 벗어나면 안 되므로). 사진이 배경
전체를 채우는 풀블리드이고, 스킬/패시브 텍스트는 완전히 뺐다 — "3초 안에 읽혀야 하는" 포맷이라는
판단. 하단에 어두운 그라데이션 스크림을 깔고 그 위에 이름·레벨·HP·거리·칭호만 얹는다.

## 시안 D: 여권/스탬프형 — `card-stamp.html` (2026-08-26)

게임 카드 문법 대신 "도장깨기 여권" 컨셉 — 개발자가 제안하고 사용자가 채택. 정보를 카드가 아니라
서류처럼 점선 리더(`····`)로 나열하고(이름/칭호/속성/거리/컨디션), 사진 모서리에 빨간 잉크 도장이
살짝 회전된 채로 걸쳐 찍힌다. 스킬은 아예 없고 패시브만 "발급 조건" 형태로 한 줄 들어간다.
`#다산아울렛_도장깨기완료` 같은 기존 배지 문구 톤이랑 잘 맞고, 아웃렛 매장별로 스탬프를 모으는
리텐션 구조로 자연스럽게 확장하기 좋은 방향이라고 판단함 — 단, 실제로 매장별 스탬프를 어떻게
모으게 할지는 아직 설계 안 됨(미정 사항의 미션/리워드 설계와 연결). 서류형 레이아웃이라 값이 너무
길면(`docRow()`) 줄임표로 잘라서 다른 열과 안 겹치게 처리해둠.

**레이아웃 개정 (같은 날, 사용자 피드백)**: 처음엔 사진을 여권사진처럼 작은 정사각형(210x210)으로
만들고 그 옆에 정보를 배치했는데, 사용자가 "카드 위쪽에만 내용이 차 있다"고 지적함 — 실제로
카드 아래 절반이 거의 비어 있었음. 사진을 훨씬 큰 가로 배너(560x300)로 키우고, 정보 줄은 사진
옆이 아니라 아래로 옮겨 카드 폭 전체를 쓰게 바꿨다. 그래도 남는 공간을 채우려고 **"도장 현황"
섹션**(장식용 — 등급값만큼 채워지는 스탬프 5개, 진짜 매장별 방문 인증이 아니라는 걸 라벨에
명시)을 추가함. 사진이 커지면서 캔버스 높이도 900→960으로 늘림(억지로 900에 욱여넣으니 도장이
아래 정보 줄과 겹치는 문제가 생겨서, 넉넉하게 늘리는 쪽을 택함).

**버그로 배웠던 것 2 (같은 날, 사진을 배너로 키우면서 생김)**: 정사각형(`PHOTO_SIZE`)에서
가로 배너(`PHOTO_W`/`PHOTO_H`)로 바꿀 때 드래그 히트테스트 함수 안에 옛날 변수명 `PHOTO_SIZE`가
그대로 남아있어서, 사진을 드래그하려고 하면 `ReferenceError`가 나서 위치 조절이 아예 안 되는
버그가 있었음 (사용자가 실사용하다 발견). 새 변수명으로 고침 — **시안 파일에서 사진 관련 변수를
리네임할 땐 `hitTest`/드래그 관련 코드까지 같이 확인할 것** (그리기 코드만 고치고 이벤트 핸들러
쪽은 빠뜨리기 쉬움).

**사진을 더 세로로 크게 (같은 날, 추가 요청)**: `PHOTO_H`를 300→480으로 늘리고 캔버스 높이도
960→1140으로 똑같이 늘림. 아래쪽 레이아웃이 전부 `PHOTO_Y + PHOTO_H` 기준 상대 좌표로 짜여 있어서,
사진 높이와 캔버스 높이를 같은 만큼만 늘리면 나머지 요소 간격은 그대로 유지된다 — 이 시안에서
사진 크기를 또 조정할 일이 생기면 이 방식(둘 다 같은 델타만큼)을 그대로 쓰면 됨.

**버그로 배웠던 것 (C/D 만들면서 공통으로 발견, 2026-08-26)**: `CardShared.attachPhotoDrag()`
내부에서 `canvas.setPointerCapture()`가 예외를 던지는 경우가 있었는데(포인터를 못 찾았다는
`NotFoundError` — 실제 기기보다는 자동화 테스트 환경에서 잘 재현됨), 잡아주는 코드가 없어서
드래그 핸들러 자체가 중간에 끊길 뻔한 걸 `try/catch`로 방어함. 실기기에서 문제가 될 확률은 낮지만
방어적으로 고쳐뒀다.

인쇄 파이프라인(도련/여백, DPI, CMYK 변환 등 실제 인쇄소 규격)은 아직 다루지 않음 — 지금은
화면에서 보이는 디자인 퀄리티만 다룬 상태.

## 사진 필터: AnimeGAN 검토 후 반려, 순수 픽셀 처리 카툰 필터로 대체 (2026-08-26)

사용자가 AnimeGAN(실제 AI 스타일 변환)을 필터로 붙일 수 있는지 물어봐서 조사한 결과, 브라우저에서
돌아가는 구현(AnimeGAN.js)은 있었지만 **원본 AnimeGAN 모델에 라이선스 파일이 없어 상업적 이용이
법적으로 애매하다는 점**과, 모델 다운로드/추론이 산책 중 쓰는 모바일 PWA에 부담될 수 있다는 점 때문에
채택하지 않기로 함 (개발자가 조사해서 비권장 의견을 냈고 사용자가 동의).

대신 AI 없이 Canvas 픽셀 처리만으로 카툰 느낌을 내는 필터를 `card-shared.js`의
`CardShared.applyCartoonEffect()`로 구현해 필터 목록에 추가함: 대비/채도를 올린 뒤 색상을 5단계로
단순화(posterize)하고, R/G/B 채널별 Sobel 엣지 검출(밝기만 보면 갈색 털 vs 초록 잔디처럼 밝기는
비슷한데 색은 다른 경계를 놓치는 문제가 있어서, 채널별로 계산 후 가장 강한 값을 사용)로 윤곽선을
검게 덧그린다. 카드에 표시하는 크기가 크지 않아 처리 속도는 사진 1장당 50ms 안팎으로 충분히 빠름.

**실제 반려동물 사진으로 테스트하다 발견한 문제 (같은 날 수정)**: 도형 몇 개짜리 테스트 이미지로만
검증했을 땐 멀쩡했는데, 잔털이 많은 실제 사진을 넣으니 털 한 올 한 올의 미세한 명암 차이가 전부
"엣지"로 잡혀서 사진 전체가 새까만 낙서처럼 나오는 문제가 있었음 (사용자가 실제 캡처로 확인해줌).
원인은 엣지 검출/색상 단순화 전에 노이즈를 죽이는 블러 단계가 빠져 있었던 것 — 블러(4px)를 추가하고,
"노이즈 낀 사진"과 "깨끗한 사진"의 엣지 검출 결과가 비슷해지는 지점으로 블러 강도/임계값을
다시 맞춰서 해결함.

## 스킬/특성 칩 여백 확대 (2026-08-27)

"스킬제목과 그 테두리 폭을 좀 더 넓히고, 스킬제목과 스킬내용 사이 폭도 조금 더 띄우자,
특성제목도 특성 내용 사이에 폭을 좀 줘"라는 요청 → `card-shared.js`의 `drawTraitChip()`은
스킬 칩과 특성(패시브) 칩이 같이 쓰는 함수라 한 곳만 고치면 둘 다 반영됨:
- `padX`(제목/설명 텍스트와 칩 테두리 사이 여백): 24 → 32
- `midGap`(제목 블록과 설명 블록 사이 간격): 6 → 14

칩 개수만큼 카드 세로 길이가 조금씩 늘어남(칩 하나당 +8px) — 시안 A(스킬 2개+패시브 1개,
총 3칩)는 +24px, 시안 B/B-1(스킬 1개+패시브 1개, 총 2칩)는 +16px, 자동 높이 맞춤 로직이
그대로 처리하므로 레이아웃이 깨지지 않음(실제 캔버스 높이 변화로 확인됨). `card-shared.js`를
고쳤으므로 캐시 버전을 v=8 → v=9로 5개 파일 전부 같이 올림.

## 카드 퀄리티 개선: 타이틀 폰트 (2026-08-26)

카드 전체가 시스템 기본 서체(맑은 고딕)라 "프로토타입" 느낌이 난다는 지적 이후, 개발자가 개선
방향 몇 가지(폰트/아이콘/홀로그램 효과/배경 패턴/그림자 — 우선순위 순)를 제안했고, 그중 폰트부터
적용하기로 함.

이름·스킬/패시브 제목·획득 칭호처럼 카드에서 눈에 띄어야 하는 큰 텍스트에 구글 폰트
**Black Han Sans**(한국 밈/유머 콘텐츠에서 자주 쓰이는 굵고 임팩트 있는 서체 — B급 감성과 잘
어울려서 선택)를 적용함. 스킬/패시브 **설명**문이나 폼 라벨 같은 작은 텍스트는 가독성을 위해
기존 서체(맑은 고딕) 그대로 유지. `card-shared.js`의 `drawTraitChip`(스킬/패시브 제목)에도
적용해서 두 시안(`card.html`, `card-keyring.html`)에 공통 반영됨 — 새 시안을 추가할 때도
`<head>`에 구글 폰트 `<link>` 3줄을 추가하고, 스크립트 마지막에
`document.fonts.load('...px "Black Han Sans"').then(renderCard)`를 넣어야 폰트 로딩 전에
그려져서 기본 서체로 굳어버리는 걸 방지할 수 있음 (캔버스는 웹폰트 로딩을 기다려주지 않는
캔버스 API 특성 — 이 패턴을 재사용할 것).

다음 후보(아직 미적용): 5성 카드 홀로그램 셰인 효과, 배경 발자국 패턴, 텍스트/요소 드롭섀도우.

## 시안 B: 스킬 제목 폰트/색상 커스터마이징 (2026-08-26)

"제목의 색깔을 조금 더 가시성있는 색깔로" 피드백에 이어, 사용자가 스킬 제목의 폰트와 색상을
직접 콤보박스(드롭다운)로 골라 바꿀 수 있게 해달라고 요청 — **범위는 스킬 제목 한정**(패시브
제목은 기존대로 고정 유지, 폼에 콤보박스도 추가하지 않음).

`card-keyring.html`에 두 개의 `<select>`(`skill-title-font`, `skill-title-color`)를 추가:
- **폰트 5종**: 맑은 고딕(기본) / Black Han Sans / 주아체 / 도현체 / 개구체 — 뒤 3개는 이번에
  새로 구글 폰트 `<link>`에 추가함(기존엔 Black Han Sans만 로드하고 있었음).
- **색상 6종**: 코랄 / 골드 / 진브라운 / 퍼플 / 포레스트그린 / 네이비.

`drawCard()`에서 선택값을 읽어 `CS.drawTraitChip()` 호출 시 `titleFont`/`titleColor`로 넘기는
방식이라, 이미 있던 옵션(위 "정보 축소 + 스킬 텍스트 중심으로 재정리" 참고)을 그대로 재사용했고
`card-shared.js` 자체는 수정 없음.

**새 폰트를 추가할 때 체크리스트** (이번에 겪은 실수 방지용): `<head>`의 구글 폰트 `<link>` href에
패밀리를 추가하는 것과, `SKILL_TITLE_FONTS` 배열에 항목을 추가하는 것 **둘 다** 해야 함 — 하나만
하면 드롭다운엔 뜨는데 실제로는 로딩 안 된 폰트라 브라우저 기본 서체로 조용히 대체되어 버림.
`document.fonts.load()`로 후보 폰트를 전부 미리 로딩해서 첫 렌더부터 깨지지 않게 함(기존 Black
Han Sans 패턴과 동일).

검증: 콤보박스 옵션 값이 배열과 일치하는지 확인, 색상을 네이비로 바꾼 뒤 캔버스 픽셀 스캔으로
`#2d4a6b`가 실제 제목 텍스트 위치(스킬 칩 영역)에서만 검출되는지 확인, 폰트를 주아체로 바꾼 뒤
동일 문자열의 `measureText` 폭이 맑은 고딕과 달라지는지(로딩 성공 여부) 확인, PNG 저장이 계속
정상 동작하는지 확인 — 스크린샷으로도 최종 한 번 더 눈으로 확인함.

**바로 이어서 확장: 특성 제목 색/크기 추가 + 색상 16종 + 폰트 크기 (2026-08-26)**: 사용자가
"특성제목 [] 빼줘, 특성 제목도 색깔 고르게 해주고 색깔 10가지 더, 폰트 크기도 각 제목에 적용"을
요청 → 세 가지를 반영:
- **특성 제목의 대괄호 표기 제거**: `'[' + val('passive-title') + ']'` → `val('passive-title')`로
  단순화. 점선 테두리만으로도 액티브 스킬과 구분되니 대괄호는 군더더기였음.
- **색상 팔레트 6종 → 16종으로 확장, 스킬/특성 제목이 공유**: 기존 `SKILL_TITLE_COLORS`를
  `TITLE_COLORS`로 이름을 바꾸고 10개(버건디/틸/올리브/슬레이트/로즈/다크레드/인디고/초콜릿/
  페트롤블루/다크그레이) 추가. 특성 제목에도 같은 목록으로 색상 `<select>`(`passive-title-color`)를
  새로 붙였다 — 기존에 하드코딩돼 있던 특성 제목 색(`#5f4d85`, 퍼플)이 목록의 몇 번째 항목인지
  찾아서 그 인덱스를 기본 선택값으로 넣어, 처음 열었을 때 예전과 똑같아 보이게 함(폼을 안
  건드리면 시각적으로 아무것도 안 바뀜).
- **폰트 크기 `<select>`를 스킬/특성 제목 각각에 추가** (`skill-title-size`, `passive-title-size`,
  둘 다 공용 `TITLE_FONT_SIZES` 목록 18~34px 6단계 공유, 기본은 기존과 동일한 23px). 줄 간격도
  고정값(28) 대신 `크기 + 5`로 같이 늘어나게 해서 큰 글씨를 골라도 줄 간격이 답답해 보이지
  않게 함. 폰트 종류(패밀리) 선택은 이번 요청 범위가 아니라서 특성 제목에는 추가하지 않음 —
  스킬 제목만 폰트 종류까지 고를 수 있고, 특성 제목은 색/크기만 고를 수 있음 (요청 문구가
  "특성제목도 색깔"이라고만 했지 폰트까지 언급하진 않았음 — 필요해지면 나중에 확장).

검증: 5개 `<select>`(스킬 폰트/색/크기, 특성 색/크기) 옵션 개수·기본 선택값이 모두 기대값과
일치하는지 확인(특성 색은 인덱스 3=퍼플, 크기는 둘 다 인덱스 2=23px로 예전 하드코딩값과 동일).
특성 제목 색을 버건디+34px로, 스킬 제목 색을 페트롤블루+18px로 각각 동시에 바꿔서 캔버스 픽셀
스캔으로 두 색이 서로 다른 위치·크기(글자 높이 15px vs 31px)로 독립적으로 렌더링되는지 확인 —
한쪽을 바꿔도 다른 쪽 색/크기가 안 바뀌는 걸로 "각각의 제목에 적용"이 제대로 됐는지 검증함.
스크린샷으로 대괄호가 실제로 사라졌는지, 두 제목의 색/크기가 눈에 띄게 다른지도 최종 확인함.
PNG 저장도 계속 정상 동작함.

**획득 칭호 글자색 가독성 수정 (같은 날)**: 획득 칭호(`🏅 #다산아울렛_...`) 줄 색이 코랄(`#d98a72`)
이었는데, 카드 몸통 배경(크림~살구 그라데이션)과 색조가 비슷해 겹쳐 보인다는 지적을 받음 —
이름 텍스트에도 쓰는 진브라운(`#4a3f35`)으로 바꿔서 배경과 뚜렷하게 대비되게 함. 메타 줄
(`Lv.3 · 공원산책형`)은 여전히 코랄을 쓰는데, 거긴 글자 크기가 작지 않고 위치도 사진 바로
아래 밝은 영역이라 지적받은 적이 없어 그대로 뒀다 — 나중에 같은 피드백이 나오면 이 줄도
검토할 것.

## 획득 칭호 카탈로그 추가 (2026-08-26)

기존엔 "획득 칭호" 필드가 그냥 자유 입력 텍스트(기본값 `#다산아울렛_도장깨기완료`, 해시태그
형식)였는데, 사용자가 산책 퀘스트와 연동할 것을 염두에 둔 실제 칭호 목록을 5개 카테고리, 총
25개 통째로 제공 → SKILLS/PASSIVES와 똑같은 "카탈로그 + 드롭다운" 패턴으로 반영했다.

- `card-shared.js`에 `BADGES` 배열 추가 (카테고리: 초로컬 & 장소 정복 / 🏃 산책 & 미션 달성 /
  🦁 비주얼 & 외모 특징 / ⚡ B급 위트 & 참지 않는 반전 / 🎀 생애 주기 & 트레이너 애착).
- SKILLS/PASSIVES와 달리 칭호는 "칭호명 (조건)"이 한 줄로 붙어있고 별도 설명 칸이 없어서,
  기존 `buildCatalogSelect`(제목+설명 2칸용)를 그대로 못 쓰고 `buildSimpleCatalogSelect`를
  새로 추가함(입력칸 1개짜리 카탈로그 전용, optgroup 그룹핑 로직은 동일).
- 4개 시안 파일 전부(`card.html`, `card-keyring.html`, `card-story.html`, `card-stamp.html`)에
  "획득 칭호" 드롭다운을 추가하고, 기존 텍스트 입력칸은 라벨을 "획득 칭호 (해시태그)" →
  "획득 칭호"로 바꿈(카탈로그 항목이 해시태그 형식이 아니라서) — `maxlength`도 30 → 50으로
  늘림(새 칭호 문구가 기존 해시태그 예시보다 길어서). `card-shared.js`를 고쳤으므로 캐시
  버전도 v=4 → v=5로 같이 올림(4개 파일 전부).
- 대괄호 표기(특성 제목에서 뺐던 것)처럼, 원래 텍스트 필드에 있던 하드코딩 기본값(`value="..."`)은
  빼고 SKILLS/PASSIVES처럼 카탈로그 첫 항목이 로드 시 자동으로 채워지게 함(직접 수정은 계속 가능).
- ⚠️ **알려진 제약**: 시안 A(`card.html`)는 칭호 줄을 22px Black Han Sans(두꺼운 대형 서체)로
  가운데 정렬해서 그리는데, 이 폰트는 글자 폭이 넓어서 새 칭호 중 긴 것(30자에 가까운 항목)을
  고르면 카드 테두리에 닿거나 살짝 넘칠 수 있다 — 칭호 줄에는 스킬/패시브 칩 같은 자동 줄바꿈이
  없기 때문. 문제가 되면 칭호 텍스트를 직접 줄여서 쓰거나(입력칸은 계속 수정 가능), 나중에 이
  줄에도 `wrapText`를 적용하는 걸 검토할 것.

## 사진 확대·축소(스크롤/핀치 줌) 추가 (2026-08-26)

사진 위치 조절(드래그)만 있고 확대는 안 되던 걸, 데스크톱은 사진 위에서 마우스 휠 스크롤로,
모바일은 두 손가락 핀치로 확대·축소할 수 있게 요청받아 4개 시안 전부에 반영.

- `card-shared.js`의 `computePhotoFit(rect, image, zoom)`에 `zoom`(기본 1) 인자를 추가 — cover-fit
  스케일에 이 배율을 곱해서, 확대했을 때 실제로 얼마나 커지고 팬 가능 범위(`maxPanX/Y`)가
  얼마나 늘어나는지 계산한다. 기존 호출부(줌 기능 없던 시절 코드)는 `zoom` 인자를 안 넘겨도
  기본값 1로 동작해서 그대로 호환됨.
- `attachPhotoDrag`를 확장: 포인터를 `pointerId`별로 추적하다가 2개가 동시에 눌리면 팬 대신
  "핀치 모드"로 전환해서 두 점 사이 거리 변화 비율로 줌을 계산한다(`getZoom`/`setZoom`을
  넘긴 경우에만 활성화 — 넘기지 않으면 기존과 동일하게 팬만 지원). 손가락 하나를 떼면 남은
  손가락 기준으로 팬을 다시 시작한다. 데스크톱용으로 `wheel` 이벤트도 같이 붙여서
  (`{ passive: false }`로 등록해 `preventDefault` 가능하게 함), 사진 영역 위에서만 휠 스크롤이
  줌으로 동작하고 밖에서는 평소처럼 페이지가 스크롤된다.
- 각 시안 파일에 `photoZoom`(기본 1) 상태와 `PHOTO_MIN_ZOOM=1`/`PHOTO_MAX_ZOOM=3`를 추가하고,
  `clampPhotoOffset()`/`drawPhoto()`의 `computePhotoFit` 호출에 `photoZoom`을 같이 넘기도록
  수정. 줌 배율이 바뀌면 팬 가능 범위도 바뀌므로, `setZoom` 콜백 안에서 `clampPhotoOffset()`을
  다시 호출해 오프셋이 범위를 벗어나지 않게 함. 새 사진을 올리면 `photoZoom`도 1로 리셋.
- `card-shared.js`를 고쳤으므로 캐시 버전을 v=5 → v=6으로 4개 파일 전부 같이 올림.
- 검증: 동심원(파랑-초록-빨강 3겹) 테스트 이미지를 만들어 업로드한 뒤, 프레임 가장자리 한 점의
  픽셀 색이 줌인할수록 바깥쪽 색(파랑) → 안쪽 색(빨강)으로 바뀌는 걸로 실제 확대가 적용되는지
  확인. 합성 `WheelEvent`(휠 줌)와 2개의 `PointerEvent`(핀치 줌) 양쪽 다 같은 결과가 나오는 걸
  확인했고, 휠을 반대로 굴리면 원래 색(줌 1배)으로 정확히 돌아오는 것(최대/최소 줌 클램프)도
  확인함. 핀치 후에도 손가락 하나로 드래그하면 팬이 정상 동작하는지, PNG 저장이 계속 되는지도
  같이 확인. 4개 파일 전부 콘솔 에러 없이 로드되는 것도 확인함.

## 카드 퀄리티 개선: 이모지 → Twemoji (2026-08-26)

이모지가 OS/브라우저마다 모양이 달라지는 문제(애플/윈도우/안드로이드가 각자 다르게 그림) 해결
방법을 사용자가 먼저 "애플 스타일로 가보자"고 제안 → 조사해보니 **애플 이모지는 완전 독점
라이선스라 iOS/macOS 밖에서 못 쓰게 돼 있음**을 확인하고 반려. 대신 상업적으로 완전히 안전한
대안(Twemoji vs Noto Emoji vs 손그림 커스텀 아이콘)을 캔버스 손그림 아이콘 샘플과 Twemoji 샘플로
같이 보여주고 비교한 뒤, 사용자가 **Twemoji**(트위터/X, CC BY 4.0)로 결정.

구현은 `card-shared.js`에 몰아넣음 (`preloadEmoji`, `getEmojiImage`, `emojiReady`, `drawEmojiLine`):
- CDN은 `cdn.jsdelivr.net/gh/twitter/twemoji@14.0.2/assets/svg/` — jsdelivr가
  `Access-Control-Allow-Origin: *`를 열어주는 걸 확인하고 씀. 이게 안 되는 CDN을 쓰면
  `crossOrigin` 없이 그린 이미지 때문에 캔버스가 "오염"돼서 "카드 이미지 저장"
  (`canvas.toBlob`)이 조용히 실패한다 — 실제로 저장 기능이 여전히 되는지 테스트로 확인함.
- 이모지는 `ctx.fillText`로 못 그리는 이미지라, 텍스트 앞에 이미지를 그리고 그 폭만큼 띄워서
  텍스트를 잇는 방식(`drawEmojiLine`)으로 구현. 스킬/패시브 칩 제목처럼 줄바꿈이 걸리는 곳은
  이모지가 첫 줄에만 붙고 폭 계산에도 반영되도록 `drawTraitChip`을 고쳤음(`titleEmoji`/`titleText`
  분리).
- 이모지 이미지는 비동기로 로딩되므로(폰트와 같은 문제) 로딩 전엔 텍스트만 그렸다가, 로딩이
  끝나면 다시 그려서 자연스럽게 나타나게 함.
- **버그로 배웠던 것**: `twemoji.convert.toCodePoint()`가 반환하는 코드포인트에 변형 선택자
  (`-fe0f`)가 붙어있어도 실제 CDN 파일명에는 빠진 경우가 있음(❤️가 대표적 — `2764-fe0f.svg`가
  아니라 `2764.svg`). 처음 요청이 404면 `-fe0f`를 뗀 이름으로 한 번 더 시도하도록 재시도 로직을
  넣어서 해결. 새 이모지를 추가할 때 이 문제가 재발할 수 있으니 참고.

## 결정된 사항

| 항목 | 결정 | 날짜 |
|---|---|---|
| 플랫폼 | 모바일 웹앱 (PWA) — 앱스토어 설치 없이 브라우저로 접근 | 2026-08-26 |
| 지도 API | ~~Mapbox 계열~~ → Leaflet + OpenStreetMap (무료, 가입/카드 등록 불필요) | 2026-08-26 → 2026-09-02 변경 |
| 카드 시각 컨셉 | 반려동물 사진 + 포켓몬 카드 감성의 "산책 카드" 이미지 생성 (아래 상세) — ~~미션 리워드로 획득~~ 조건 없이 언제든 만드는 기록 도구로 재정의(2026-09-02, 아래 "보상 폐기" 참고) | 2026-08-26 |
| 톤앤매너 | B급 감성 — 카드 스킬 텍스트를 "제목(드립 스탯) + 부연 설명" 2단 구조로 (위 "톤앤매너" 참고) | 2026-08-26 |
| 카드 시안 관리 방식 | 시안별로 파일 분리(`card.html`, `card-keyring.html`, ...), 공통 로직은 `card-shared.js`로 공유 (위 "카드 시안을 여러 파일로" 참고) | 2026-08-26 |
| 카드 시안 4종 | A 기본형(전체 스탯) / B 키링형(실물 출력) / C 스토리 공유형(9:16, SNS용) / D 여권·스탬프형(도장깨기 컨셉) | 2026-08-26 |
| 핵심 가치제안 | A(즉석 카드 기념품) 단독 집중, B(동네 소셜/실시간 열람)는 통째로 보류 (아래 "서비스 다이어트" 참고) | 2026-09-02 |
| 카드 시안 우선순위 | C(스토리형, `card-story.html`)를 실제 서비스 기본 시안 1순위 후보로 추천 — 5개 파일 전부 삭제하지 않고 보관, 최종 채택은 사용자 판단 대기 | 2026-09-02 |
| 산책 보상 | ~~산책 카드 생성 하나로 한정~~ → **보상 개념 자체를 폐기.** 카드는 "달성해서 얻는 것"이 아니라 조건 없이 언제든 만들 수 있는 산책 기록/창작 도구로 재정의 (아래 "보상 폐기" 참고) | 2026-09-02 (같은 날 재변경) |
| 치팅 방지 | 논의 종료 — 보상이 없으니 부정 획득할 대상 자체가 없음 | 2026-09-02 |
| 스트릭(연속 산책일수) | 유지하되 **압박/페널티 없는 순수 통계**로만 둠 — 안 걸어도 벌점 없이 "이번 달 N번 걸었어요" 식 정보성 기록 | 2026-09-02 |

### 이 결정이 의미하는 제약 (개발자 참고용 — 확정 스펙 아님)

- PWA 특성상 **백그라운드 GPS 추적은 iOS/Android 네이티브 앱만큼 안정적이지 않다.**
  브라우저 탭/화면이 꺼지면 위치 추적이 끊기거나 정확도가 떨어질 수 있음. 초기 프로토타입은
  "화면을 켜둔 채 산책" 전제로 만들고, 실사용 테스트 후 이 부분이 문제가 되면 네이티브 전환을
  다시 논의해야 한다.
- (2026-09-02 갱신) Mapbox는 배제하고 **Leaflet + OpenStreetMap**으로 전환해서 가입/토큰 없이
  바로 지도가 뜬다 (아래 "지도 API 변경" 참고). 다만 공개 OSM 타일 서버(`tile.openstreetmap.org`)는
  상업적/대량 트래픽 사용을 금지하는 이용 정책이 있어서, 프로토타입 단계를 넘어 실제 서비스로
  트래픽이 늘면 유료 타일 공급자(MapTiler, Stadia Maps 등)나 자체 호스팅으로 옮겨야 한다.

### 산책 카드 (미션 리워드) 상세 — 2026-08-26 사용자가 별도로 구상해온 디자인

포켓몬 카드처럼 반려동물 사진에 감성 필터를 입히고, 그 위에 스탯 표를 얹은 카드 이미지를 만든다.
사용자가 다른 AI와의 대화에서 잡아온 카드 구성 예시를 그대로 채택:

- 카드 프레임: 3D/파스텔 일러스트 프레임, 등급(별점 ★로 표시), 속성 태그 (예: `공원산책형`)
- 반려동물 실제 사진에 필터를 입혀 3D/카드 일러스트 느낌으로 가공
- 이름 + 레벨 (예: `뽀삐 (Level 3 - 다산 탐험가)`)
- HP 바 (체력 게이지 형태)
- 산책 거리 (오늘 / 누적)
- 스킬 1·2 (예: `킁킁 인사`, `간식 갈구` 같은 반려동물 행동을 게임 스킬처럼 표현)
- 획득 칭호 (해시태그 형태, 예: `#다산아울렛_도장깨기완료`)

**(2026-09-02 갱신) "달성 조건" 관점의 미정 사항은 해소됨 — 보상 폐기 결정으로 더 이상
추적하지 않음** (아래 "최종 결정: 보상 개념 자체를 폐기" 참고). HP·레벨·스킬 획득 조건,
카드 등급/레어도 기준 같은 게임 디자인 질문은 애초에 "무엇을 달성해야 주는가"를 정하는
문제였는데, 달성/보상 구조 자체가 없어졌으니 더 이상 답할 필요가 없는 질문이 됨. 칭호
목록(`card-shared.js`의 `BADGES`, 25개)은 여전히 카탈로그로 남아있지만 "달성 조건"이 아니라
그냥 카드 만들 때 골라 넣는 문구 소재로만 취급한다.

프로토타입 단계에서는 위 값들을 화면에서 직접 입력하는 폼으로 만들어 "카드가 어떻게 보이는지"만
먼저 검증한다 (`card.html`). 카드는 산책 기록/창작 도구이지 미션 리워드가 아니므로, 실제 산책
데이터 자동 연동은 "카드 생성 흐름 자동화"(사진만 고르면 거리/시간이 자동으로 채워지는 것,
위 "서비스 다이어트" 참고) 관점에서만 진행하면 된다.

## 미정 사항 (구현 전 확인 필요 — 임의로 진행하지 않음)

- **회원/인증**: 누가 로그인하고, 반려동물 프로필은 어떻게 등록하는지 (계정 시스템 자체가 미정)
- **백엔드/저장소**: 여러 사용자의 경로·핀·미션 데이터를 실시간으로 주고받아야 하므로 `projectA`의
  "서버 없이 브라우저만" 전제와 달리 **이 프로젝트는 처음부터 서버/DB가 필요할 가능성이 높다.**
  다만 어떤 백엔드(자체 서버, Supabase, Firebase 등)를 쓸지는 아직 미정.
- **공유 범위/프라이버시**: (2026-09-02) B(동네 소셜/실시간 열람) 자체가 통째로 보류되면서 이
  항목도 함께 보류 — B를 다시 살릴 때 재검토
- **핀의 종류/의미**: (2026-09-02) 핀은 공유/열람용이 아니라 개인용 메모로만 유지하기로
  정리됨(위 "서비스 다이어트" 참고) — 카테고리 분류가 필요한지는 여전히 미정이지만 우선순위는
  낮음
- ~~**미션/리워드 설계**~~: (2026-09-02) **해소됨.** 보상 개념 자체를 폐기하기로 결정하면서
  히든 미션·달성 조건·리워드 매핑 공식 같은 질문이 전부 무의미해짐(위 "최종 결정: 보상 개념
  자체를 폐기" 참고). 카드는 산책 기록/창작 도구로 재정의됨.
- **수익 모델**: 아웃렛 매장과의 제휴/광고 여부 (사업 배경상 가능성 있어 보이나 미확인) — 보상
  폐기로 치팅 방지는 더 이상 이 결정에 걸려있지 않지만, 매장 제휴/쿠폰 자체의 방향은 여전히
  미정

## 지도 API 변경: Mapbox → Leaflet + OpenStreetMap (2026-09-02)

Mapbox는 무료 티어도 가입 시 카드 등록이 필요해서 사용자가 반감을 느껴 배제하기로 결정 →
[index.html](index.html)을 Mapbox GL JS에서 Leaflet + OpenStreetMap 타일로 전면 교체함
(가입/토큰 없이 바로 지도가 뜸). GPS 경로 기록·핀 찍기 기능은 지도 라이브러리와 무관하게
동작하는 로직이라(`watchPosition`으로 좌표를 모아 선을 그리는 것뿐) 그대로 이식됨 — Mapbox
GeoJSON 소스/레이어 → Leaflet `L.polyline`, `mapboxgl.Marker` → `L.marker`+`L.divIcon`로 교체.
좌표 순서가 Mapbox는 `[lng, lat]`, Leaflet은 `[lat, lng]`로 반대라 이 부분 버그에 주의해서 옮김.

**트레이드오프 (개발자 참고용)**: 공개 OSM 타일 서버는 상업적 대량 트래픽을 금지하는 이용
정책이 있어 프로토타입 단계에는 문제없지만, 실제 서비스로 트래픽이 늘면 유료 타일 공급자나
자체 호스팅으로 옮겨야 한다. 스타일 커스텀도 Mapbox Studio 같은 레이어별 색상 지정이 아니라
CSS `filter`(hue-rotate/invert 등)로 전체 타일에 한번에 걸어야 해서 세밀한 제어가 어렵다 —
`map-color-test.html`에서 5가지 톤(모노톤 그레이/쿨 파스텔 민트/세피아 빈티지/다크 네이비/
라벤더 파스텔)을 만들어 비교해봤고, 아직 최종 톤은 미정.

**버그로 배웠던 것**: Leaflet은 내부 지도 레이어들(`leaflet-tile-pane` 등)에 z-index
200~700을 씀. `#map`에 `position: absolute`만 있고 `z-index`가 없으면(auto) 자체 스태킹
컨텍스트가 안 생겨서, 이 내부 레이어들이 상단바/하단바(z-index: 10)보다 위로 뚫고 올라와
UI가 안 보이는 문제가 있었음 — `#map`에 `z-index: 0`을 줘서 자체 스태킹 컨텍스트를 만들어
해결함([index.html](index.html)의 `#map` 규칙). 앞으로 지도 위에 다른 UI 레이어를 얹을 때
이 패턴(지도 컨테이너에 명시적 z-index 지정)을 참고할 것.

## 서비스 다이어트: 핵심 가치제안 좁히기 (2026-09-02)

사용자가 "서비스가 헤비해졌다"며 재정리를 요청함. 카드 시안 5종·칭호 25개·지도 연출 3종처럼
표면 기능은 계속 늘었는데, 정작 "왜 산책 중 이 앱을 켜야 하는가"에 대한 답이 한 번도 명시적으로
정리된 적이 없었음 — 핵심 가치제안을 정하고 과감히 쳐낼 것/보류할 것을 구분함.

### 핵심 가치제안: A(즉석 카드 기념품) 하나로 집중, B(동네 소셜/실시간 열람)는 통째로 보류

기존 원안(이 문서 최상단 "핵심 기능")은 사실 성격이 다른 두 서비스가 섞여 있었음:
- **A. 즉석 기념품**: 산책이 끝나면 카드가 자동으로 생김 — 지금까지 만든 카드 시스템이 여기 해당
- **B. 동네 소셜/열람**: 핀 공유, 다른 유저 경로 열람, 실시간 동네 친구 카운트 — 전부 회원/백엔드/
  멀티유저가 전제라 콜드스타트 문제(초기 유저가 없으면 지도가 텅 빔)가 프로토타입 단계에서는
  검증 자체가 불가능함

→ **A만 남기고 B는 통째로 보류**하기로 결정. `index.html`의 핀 찍기 기능은 이미 "그 자리에서
로컬 마커만 찍고 저장하지 않는" 수준이라 실제로 손댈 코드는 없음 — 핀은 앞으로도 개인용 메모로만
유지하고, 공유/열람 관련 기능은 새로 만들지 않는다는 뜻.

핵심 루프를 다음으로 재정의(아직 미구현, 로드맵):
```
산책 시작 → GPS 경로 기록 → 산책 종료
   → 카드 화면으로 자동 이동, 거리/시간 값이 자동으로 채워짐 (현재는 수동 입력)
   → 사진 한 장만 고르면 스킬/패시브/칭호는 카탈로그에서 자동 랜덤 배정
   → "다시 뽑기"로 리롤 가능 → 저장/공유
```
지금 카드 화면들의 폰트/색상/크기 select 5~6개는 개발자가 시안을 비교해보려고 만든 도구이지
최종 사용자 UX가 아님 — 기본 흐름은 "탭 한 번 = 완성"으로 자동화하고, 기존 커스터마이징 UI는
지우지 않되 "고급 설정(펼치기)" 뒤로 숨기는 방향으로 간다(아직 미구현).

### 카드 시안: C(스토리형)를 1순위 후보로, 나머지는 파일 그대로 보관

5개 시안 파일(`card.html`, `card-keyring.html`, `card-keyring-b1.html`, `card-story.html`,
`card-stamp.html`)은 하나도 삭제하지 않고, **C(스토리형, `card-story.html`)를 실제 서비스의
기본 시안 1순위 후보**로 추천함(최종 채택 여부는 사용자 판단 대기):
- 수동 입력 필드가 0개(스킬/패시브 텍스트 자체가 없음)라 "탭 한 번 = 완성" 자동 흐름과 가장
  잘 맞음
- 9:16 SNS 스토리 전용으로 이미 설계돼 있어 "산책 끝나자마자 인스타에 자랑"이라는, 앱 밖에서
  작동하는 동기부여를 자연스럽게 만들어줌 — "왜 켜야 하는가"에 대한 가장 직접적인 답
- 트레이드오프: 지금까지 공들인 B급 감성 스킬 텍스트가 카드에 안 보임. 아쉬우면 C를 기본으로
  하되 B/B-1의 스킬 카드를 "더보기"로 이어 보여주는 2장 세트 구성도 대안으로 남겨둠

### 리워드: 카드 하나로 한정 (2026-09-02, 같은 날 재검토 후 폐기됨 — 아래 참고)

처음엔 "산책 시 받는 보상은 산책 카드 생성 하나뿐"로 리워드를 최소화하는 선에서 정리했었다.
카드는 교환가치가 없는 개인 소장 이미지라 가짜로 만들어도 얻는 게 자기만족뿐이니, 치팅
유인 자체가 없다는 논리였음. **이 결론은 "카드가 1회성 소모품"이라는 전제 위에서만
성립한다** — 아래 "카드가 질릴 것 같다" 논의를 거치며 이 전제가 깨져서 최종적으로
폐기됨(바로 아래 "보상 폐기" 참고).

### (중간 검토 후 폐기) 카드를 성장형 자산으로 바꾸는 안

카드가 매번 새로 뽑는 소모품이라 반복하면 질릴 거라는 우려에, 카드를 반려동물당 하나로 두고
산책 데이터가 쌓일수록 레벨업·스킬 해금되는 "성장형 카드"(포켓몬/다마고치식) 안을 검토했었다.
그런데 이 안은 리워드를 "1회성"에서 "누적 성장"으로 바꾸는 것이라, 치팅 방지 결론의 전제
("가짜로 만들어도 허무할 뿐이라 유인이 없다")를 깨버림 — **걷지도 않고 스킬이 풀리면 판정
기준이 허술한 앱으로 보여 평점이 낮아질 것**이라는 지적을 받음(죄책감이 아니라 제품 신뢰도
문제). 이를 막으려면 스킬 해금처럼 "보여주고 자랑하는" 요소에 현장 사진 촬영 + 온디바이스
강아지 인식(TensorFlow.js 등, 서버 없이 클라이언트에서 가능) 같은 실제 마찰이 필요하다는
데까지 논의가 이어졌으나, **이 안 자체가 다음 단계에서 통째로 폐기**되었으므로 이 마찰
장치들은 구현하지 않는다. (실제로 만든 코드는 없어서 되돌릴 것도 없음 — 대화 상 제안이었을 뿐.)

### 최종 결정: 보상 개념 자체를 폐기 (2026-09-02)

리워드를 걸면 반드시 "정당하게 땄는지" 판정 문제가 따라오고, 그 판정은 GPS/사진만으로는
완벽할 수 없어 사용자가 야박한 평가를 하게 된다는 지적 → **치팅을 막으려 애쓰는 대신 보상
자체를 없애기로 결정.** 리워드가 없으면 부정 획득할 대상도 없어져서 치팅 방지 논의 자체가
필요 없어짐.

- **카드는 "달성해서 얻는 리워드"가 아니라 조건 없이 언제든 만들 수 있는 산책 기록/창작
  도구로 재정의**한다. 포켓몬 카드 뽑기가 아니라 다이어리 한 페이지에 가까운 개념 — 매번
  안 질리는 이유는 새로운 걸 얻어서가 아니라 "오늘 이 산책, 이 사진을 담은 유일한 기록"이라는
  데서 온다.
- HP·레벨·스킬 해금 같은 "성장 조건"/"달성 조건" 아이디어는 전부 폐기. 아울렛 스탬프/여권
  컨셉(시안 D)도 "달성 보상"이 아니라 "오늘 여기 왔었다" 정도의 방문 기록 스티커로만 남을 수
  있음(선택 사항, 강제 아님).
- **예외: 스트릭(연속 산책일수)은 유지**하되, "안 걸으면 벌점/기록 끊김" 같은 압박 없이
  "이번 달 N번 걸었어요" 식의 **순수 정보성 통계**로만 보여준다 — 달성/실패 판정이 없으니
  치팅 문제와 무관함.
- 결과적으로 치팅 방지 로직(GPS 속도 필터 포함)은 만들지 않는다 — 지킬 보상이 없으니 방어할
  이유도 없음.

## 카드 생성 흐름 자동화 + 스트릭 구현 (2026-09-02)

위 "서비스 다이어트"에서 정리한 방향(보상 폐기, 사진만 고르면 완성되는 자동 흐름, 압박 없는
스트릭)을 실제로 구현하고 브라우저에서 검증함.

- **`index.html`**: 산책 종료 시 `routeCoords`(GPS 좌표열)로 하버사인 공식을 이용해 이동
  거리(km)를 계산하고, 시작·종료 시각으로 소요 시간(분)을 계산해 `localStorage`에 저장
  (`petwalk_walks`: 날짜별 산책 기록 배열, `petwalk_last_walk`: 카드 화면이 읽어갈 최근 산책
  1건). 종료 직후 "🎴 카드 만들기" 버튼이 나타나 `card-story.html`로 바로 이동할 수 있음.
  앱을 다시 열었을 때(대기 중 상태)도 "이번 달 N번 산책 · 연속 N일째"를 상단에 표시함 — 이건
  달성/실패를 판정하지 않는 순수 통계라, 스트릭이 끊겨도 페널티 문구 없이 조용히 0으로 보여줌
  (위 "최종 결정: 보상 개념 자체를 폐기" 원칙 그대로).
- **`card-story.html`**: 진입 시 `petwalk_last_walk`가 있으면 "오늘 산책 거리"/"누적 산책
  거리" 필드를 자동으로 채워서 사용자가 숫자를 직접 입력할 필요가 없게 함. 획득 칭호는 매번
  카탈로그에서 랜덤으로 하나 골라 채워두고, "🎲 다시 뽑기" 버튼으로 즉시 바꿀 수 있음 —
  달성 조건이 아니라 카드 문구 소재일 뿐이므로 자유롭게 다시 뽑아도 됨.
- **검증**: `navigator.geolocation.watchPosition`을 가짜 좌표로 모킹해 실제 산책 없이 흐름을
  재현 — 산책 종료 후 상태 텍스트("0.3km · 1분 · 연속 1일째")와 "카드 만들기" 버튼 노출을
  확인하고, 그 버튼을 눌러 `card-story.html`로 넘어갔을 때 거리 필드 자동 채움과 칭호 랜덤
  배정/리롤이 정상 동작함을 브라우저에서 직접 확인함. 콘솔 에러는 기존에도 있던 Twemoji 404
  재시도(문서에 이미 기록된 정상 동작) 외엔 없었음.
- GPS 속도 필터 같은 치팅 방지 로직은 넣지 않음 — 보상이 없어 지킬 이유가 없다는 결정을 그대로
  따름.

**톤 선택 (2026-09-02, 해결됨)**: `map-color-test.html`의 5종 중 **라벤더 파스텔**
(`saturate(55%) hue-rotate(220deg) brightness(112%) contrast(92%)`)을 골라 `index.html`의
`.leaflet-tile-pane`에 반영함 — 기존에 정해져 있던 "파스텔톤 지향" 방향과 가장 잘 맞아서 선택.
다른 톤으로 바꾸고 싶어지면 `map-color-test.html`의 `TONES` 값 중 하나로 이 filter만 교체하면 됨.

## 산책 루트 추천 기능 (2026-09-07 추가)

"산책 루트를 추천해주는 기능"이 필요하다는 요청에, 추천 방식을 세 가지로 나눠 확인함
(① 다산동 큐레이션 루트 / ② 내 과거 산책 기반 재추천 / ③ 다른 유저 인기 루트 기반) — 사용자가
①을 선택. ②는 데이터가 쌓이기 전(신규 유저)엔 추천할 게 없는 콜드스타트 문제가 있고, ③은
"서비스 다이어트"(위 참고)에서 이미 보류한 B(동네 소셜) 영역이라 백엔드 없이는 불가능해서
제외됨 — ①만 지금 프로토타입 단계와 맞음.

- `index.html`에 `RECOMMENDED_ROUTES`(정적 배열: 이름/설명/좌표 루프) 3개를 추가하고
  ("다산 중앙공원 한 바퀴", "수변공원 산책로", "아울렛 둘레길"), "🌿 추천 루트" 버튼으로
  지도 위에 초록 점선 폴리라인 + 🌿 깃발 마커(시작점)를 토글 표시한다. 깃발을 클릭하면
  이름/설명/거리/예상 소요시간이 팝업으로 뜬다.
- 위 "지도 연출 기획" 섹션의 1번(추천 산책로 강조) 아이디어와 같은 방향이지만, 그때는 순수
  시각 효과(반짝임/깃발)로만 논의됐던 것을 실제 "추천 루트 보기" 기능으로 구체화한 것.
- **거리/예상 시간은 하드코딩하지 않고 좌표로부터 계산**함 — 기존 `computeRouteDistanceKm`
  (하버사인 공식, GPS 실측 거리 계산에 쓰던 것)을 그대로 재사용하고, 예상 시간은
  `WALK_SPEED_KMH = 3.5`(반려동물과 걷는 여유 속도 가정치) 기준으로 환산한다. 이렇게 해두면
  나중에 좌표를 실제 값으로 바꿔도 표시되는 거리/시간이 자동으로 맞는다.
- ⚠️ **좌표는 전부 예시값**: 지도 중심 좌표(37.5985, 127.1467, `initMap()` 주석에 이미
  "정확한 좌표는 추후 확인 필요"로 표시돼 있던 값) 주변에 임의로 만든 루프라 실제 다산
  중앙공원·수변공원 지형과 무관하다. 정확한 공원 경계/보행로 좌표가 확인되면 `RECOMMENDED_ROUTES`
  좌표만 교체하면 됨(계산 로직은 안 바뀜).
- 검증: 브라우저에서 "추천 루트" 버튼으로 3개 루트가 지도에 표시되는지, 버튼 텍스트가
  "추천 루트 숨기기"로 토글되는지, 깃발 클릭 시 팝업에 이름·설명·거리(예: 0.8km)·예상 시간
  (예: 약 13분)이 정확히 뜨는지 확인함. 콘솔 에러 없음.

### 견종 크기별 코스 추천 고도화 (2026-09-07, 같은 날 이어서)

"코스가 얼마나 걸릴지 알려주고, 견종마다 다르게 코스를 고를 수 있게" 요청 → 실제 품종별
보행 속도 데이터베이스는 없어서(프로토타입 범위 밖) **소형/중형/대형견 3단계 크기 프로필**로
근사하기로 함(개발자 판단):

- `DOG_SIZE_PROFILES`: 소형견(2.5km/h, 권장 최대 1.0km) / 중형견(3.5km/h, 2.0km) /
  대형견(4.5km/h, 4.0km) — 각 대표 견종 예시를 라벨에 병기(예: "소형견 (말티즈·포메라니안·
  치와와 등)").
- 하단바 위에 "🐕 우리 아이 크기" 선택 패널을 추가했고, **추천 루트를 켰을 때만 나타남**
  (평소엔 숨겨서 "가벼운 기능"으로 유지 — 위 "회원/로그인 전략" 원칙과 같은 결로, 이 선택도
  로그인 없이 `localStorage`(`petwalk_dog_size`)에만 저장됨).
- 크기를 바꾸면 각 루트의 예상 소요 시간이 즉시 재계산되고, 선택한 크기의 권장 거리를 넘는
  루트는 선 색이 초록(적합) → 주황(무리가 될 수 있음)으로 바뀌고 팝업에도
  "⚠️ OO견에게는 다소 길 수 있는 코스예요" 문구가 붙는다 — "코스를 고를 수 있게" 요청을
  지도 위에서 바로 보이는 시각적 구분으로 구현.
- **버그로 배웠던 것**: `localStorage.getItem()`을 스크립트 최상단에서 `try/catch` 없이
  바로 호출했더니, 스토리지 접근이 막힌 환경(브라우저 프리뷰 도구의 `data:` URL 렌더링에서
  재현됨 — 실기기에서도 프라이빗 모드 등에서 발생 가능)에서 예외가 던져져 스크립트 전체가
  멈추고 버튼이 하나도 동작하지 않는 문제가 있었음. 기존 `loadWalks()`가 이미 쓰던 패턴
  (`try/catch`로 감싸고 실패 시 기본값)을 그대로 적용해 해결 — **스크립트 파싱 시점(최상단,
  함수 밖)에서 실행되는 `localStorage` 접근은 특히 위험**하다는 걸 확인함(함수 안에서 실패하면
  그 기능만 죽지만, 최상단에서 실패하면 전체가 죽음).
- 세 루트의 거리를 일부러 짧음(0.2km)·중간(1.3km)·김(2.2km)으로 벌려서 소형견 기준으로는
  중간/긴 루트 둘 다 경고가 뜨고, 중형견 기준으로는 긴 루트만 경고가 뜨고, 대형견은 전부
  적합하게 나오도록 검증함(처음엔 세 루트가 다 0.2~0.8km로 비슷해서 소형견 기준(1.0km)을
  넘는 경우가 아예 없었음 — 값을 조정해서 실제로 갈리는지 확인).

## 지도 확대/축소 +/- 버튼 추가 (2026-09-07)

기존엔 휠 스크롤/핀치로만 줌이 가능했는데(`zoomControl: false`로 기본 버튼을 꺼둔 상태),
명시적인 +/- 버튼도 요청받아 Leaflet 기본 줌 컨트롤(`L.control.zoom`)을 켬. 기본 위치(좌측
상단)는 "🐾 산책 다이어리" 제목과 겹쳐서 우측 상단(`topright`)으로 옮겼고, top-bar(제목/상태/
카드 링크, 높이 약 54px)와도 겹치지 않도록 `.leaflet-top.leaflet-right { margin-top: 54px; }`로
그 아래에 오게 조정함. "카드" 링크 바로 아래, 지도 우측 여백에 위치.

## 추천 루트 선 부드럽게(코너 스무딩) (2026-09-07)

"추천루트 테두리선을 좀 더 부드럽게" 요청 → 처음엔 Leaflet 폴리라인에 `lineJoin`/`lineCap:
'round'`를 추가했는데, 확인해보니 **Leaflet 기본값이 이미 둘 다 'round'**라 아무 효과가
없었음(SVG 속성으로 직접 확인) — 실제 원인은 지도에 그리는 예시 루트 좌표 자체가 사각형처럼
직각으로 꺾이는 모양이라, 꺾이는 지점 하나하나가 둥글어도 경로 전체는 여전히 각져 보였던 것.

- `index.html`에 `chaikinSmooth(points, iterations)` 함수를 추가(외부 라이브러리 없이 순수
  JS, Chaikin's corner-cutting 알고리즘 — 각 변의 25%/75% 지점에 새 점을 찍어 원래 꼭짓점을
  대체하는 걸 반복해서 각진 경로를 둥글게 깎아냄, 반복 3회 적용). 지도에 그리는 `L.polyline`
  좌표에만 이 함수를 적용하고, **거리/예상 시간 계산은 계속 원본 좌표(`route.coords`)로
  한다** — 스무딩된 좌표로 다시 계산하면 (아주 조금이지만) 실제 거리와 달라질 수 있어서,
  "보여주는 선"과 "계산에 쓰는 좌표"를 분리해둠.
- 검증: 브라우저에서 SVG `path`의 좌표 개수가 원래 4~6개 꼭짓점에서 20~30개로 늘어난 것을
  확인(스무딩 적용됨), 스크린샷으로 사각형이던 루트 3개가 둥근 타원/스타디움 모양으로 바뀐
  것을 확인. 콘솔 에러 없음.

## 추천 루트를 "영역/직선"이 아니라 실제 도로를 따라가게 변경 (2026-09-07, 같은 날 이어서)

"영역 표시 말고 길을 따라서" 요청 — 지금까지의 루트는 좌표 몇 개를 이은 직선/둥근 근사 도형일
뿐 실제 도로와 무관했음. 실제 도로망을 따라가는 경로를 그리려면 라우팅 엔진이 필요해서, 가입/키
없이 쓸 수 있는 **OSRM 공개 데모 서버**(`router.project-osrm.org`, 도보 프로필)를 붙임 —
공개 OSM 타일 서버를 쓰기로 한 결정과 같은 결의 트레이드오프(아래 참고).

- `RECOMMENDED_ROUTES`의 `coords`를 `waypoints`로 이름을 바꿨다 — 이제 이 좌표들은 화면에
  그리는 선이 아니라 "이 지점들을 순서대로 지나는 실제 도로 경로를 그려줘"라고 OSRM에 넘기는
  경유점일 뿐이다. 실제로 그려지는 좌표(`route.geometry`)는 `fetchRoadRoute()`가 OSRM
  `/route/v1/foot/` 엔드포인트를 호출해 받아온다(`overview=full&geometries=geojson`).
- **비동기 전환**: 도로 경로를 받아오는 게 네트워크 호출이라 `buildRecommendedLayer()`가
  Promise를 반환하도록 바뀌었고, "🌿 추천 루트" 버튼을 처음 누르면 "🌿 불러오는 중..."으로
  바뀌며 버튼이 잠깐 비활성화된다(중복 클릭 방지). 로딩 중 사용자가 다시 껐다 켰다 해도 안전
  하도록 `recommendedVisible` 최신 상태를 콜백 안에서 다시 확인함.
- **캐시**: 한 번 받아온 도로 경로는 `route.geometry`에 캐시해서, 견종 크기를 바꿔 다시 그릴
  때는 네트워크 재호출 없이(거리/시간 재계산만) 즉시 반영된다.
- **실패 대비**: OSRM 호출이 실패(네트워크 오류/응답 이상)하거나 6초 안에 안 끝나면
  (`AbortController`로 타임아웃 처리) 이전에 만들어둔 `chaikinSmooth()` 기반 둥근 근사
  경로로 조용히 대체한다 — 이때만 선을 점선(`dashArray`)으로 그리고 팝업에도
  "(실제 도로 경로를 불러오지 못해 대략적으로 표시)" 문구를 붙여서, 사용자가 실제 도로
  경로인지 근사치인지 구분할 수 있게 함.
- **거리/시간이 더 정확해짐(부수 효과)**: 직선 기준 거리 대신 실제 도로를 따라간 거리로
  계산되면서, 예를 들어 "아울렛 둘레길"은 0.19km(직선 사각형 둘레) → 0.6km(실제 보행로 기준)로
  더 현실적인 값이 됨.
- ⚠️ **트레이드오프**: `router.project-osrm.org`는 이름 그대로 데모/테스트용 공개 서버라
  요청량이 많아지면 느려지거나 거부될 수 있고, 실제 서비스로 트래픽이 늘면 자체 호스팅 OSRM이나
  유료 라우팅 API(Mapbox Directions 등, 단 카드 등록 이슈는 위 "지도 API 변경" 참고)로 옮겨야
  한다 — 공개 OSM 타일 서버와 완전히 같은 성격의 제약이라 같은 절에서 함께 추적할 것.
- 검증: 브라우저에서 버튼 클릭 시 "불러오는 중..." → "숨기기"로 정상 전환되는 것, 지도 위 선이
  더 이상 각진 도형이 아니라 실제 도로망 모양(다리를 건너고 도로를 따라 꺾이는 등)을 따라
  그려지는 것을 스크린샷으로 확인. SVG 경로 세그먼트 수가 이전 20~30개에서 90개 이상으로
  늘어난 것(도로 형태를 촘촘히 따라간다는 증거), `stroke-dasharray`가 세 루트 모두 `null`인
  것(=폴백 없이 전부 실제 도로 경로로 성공했다는 뜻)을 확인. 팝업 거리 값이 실측에 맞게
  늘어난 것도 확인. 콘솔 에러 없음.

## 회원/로그인 전략: "로그인 없이 먼저, 차차 유도" (2026-09-07)

"미정 사항"의 회원/인증 항목(계정 시스템 자체는 여전히 미정) 중, 적어도 **진입 장벽에 대한
방향성**은 정해짐: 가벼운 기능은 로그인 없이 바로 쓸 수 있게 열어두고, 저장/개인화처럼 계정이
있어야 의미 있는 지점에서 자연스럽게 로그인을 유도하는 쪽으로 간다.

- 위 "산책 루트 추천 기능"은 이미 이 원칙에 맞게 만들어짐 — 버튼 하나로 로그인 없이 바로
  추천 루트를 볼 수 있음.
- **아직 미정인 부분**: 로그인을 "언제, 어떤 액션에서" 유도할지(예: 추천 루트를 N번 본 뒤?
  루트를 저장/즐겨찾기하려 할 때?), 로그인하면 구체적으로 뭐가 좋아지는지(즐겨찾기 저장?
  내 산책 기록 기반 추천?)는 정해지지 않음 — 실제 계정 시스템이 붙기 전까지는 UI 힌트(배너,
  버튼 등)조차 무엇을 약속하는 문구로 넣을지 알 수 없어서 구현하지 않았음. 계정 시스템 스펙이
  구체화되면 이 지점부터 다시 논의.

**프로필은 "개 중심", 견주 이름은 넣지 않는다 (2026-09-07)**: 나중에 프로필 사진 등록
기능(로그인/계정 시스템의 일부)을 만들 때 지켜야 할 원칙 — 프로필은 반려동물(사진/이름)
중심으로 구성하고, 견주(보호자) 이름은 항목으로 넣지 않는다. 지금 코드베이스에는 아직 견주
이름을 받는 필드 자체가 없어서(확인함 — 카드 화면들도 반려동물 이름/사진만 다룸) 당장 고칠
곳은 없고, 앞으로 프로필/계정 관련 폼을 설계할 때 이 원칙을 기본값으로 삼는다.

## 다음에 필요한 것 (사용자 액션)

- 파스텔톤 스타일 예시/레퍼런스(색감, 원하는 분위기)가 더 있으면 위 톤을 더 다듬을 때 참고
- 카드 시안 C(스토리형)를 기본 시안으로 최종 확정할지 — 지금 자동 흐름은 이미 C를 목적지로
  연결해뒀음(위 "카드 생성 흐름 자동화" 참고), 최종 확정 여부만 남음
